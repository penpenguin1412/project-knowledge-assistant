# 界面与流程验收

时间：2026-09-11。应用：http://127.0.0.1:8765 。验收依据：用户交接中的功能要求，以及 docs/PRODUCT.md 的验收口径。没有指定外部设计稿，本次核对实现范围及功能状态，不宣称像素级复刻任何网站。

## 已通过

- 浏览器实际创建项目、选择本机示例 MD 文件上传，成功显示 19 个片段；不是只测试接口。
- 示例项目载入两份自编虚构资料；问交付物时返回原文及位置，展开资料弹窗能看到对应行。
- 资料归档后退出检索列表，点击恢复后重新可检索；原文没有被删除。
- 切换到其他项目时资料、清单、草稿、记录随项目更新，并清除上个项目的回答。
- 明确标注原文检索不是模型回答；选择未配置的真实模型会显示错误，本次回答区域不会残留上次成功结果。
- 填入示例会议并规则提取：生成三条草稿，待办数仍为 0；未知字段为空。
- 勾选人工核对并确认后生成三条待办；修改负责人为林悦、状态为进行中，刷新后仍保留。
- 运行记录页面显示实际成功及失败记录，保留 evidence / rules / llm 的区分。
- 加载中可观察到提示和禁用输入，空项目/无资料/无待办都有可见引导。后端失败显示可操作的错误。
- 桌面 1366×900 与手机 390×844 断点实际检查；手机待办字段改为单列，DOM 测量未发现页面横向溢出。
- 前端 JavaScript 语法检查通过；浏览器日志检查未发现 JavaScript 错误。

截图：[桌面问答](../docs/screenshots/desktop.png)、[手机待办](../docs/screenshots/mobile.png)。临时浏览器视口覆盖已恢复。

## 自动化验证

`python tests.py`：20 项测试通过（2026-09-11），涵盖项目隔离、资料归档、重复导入、空/损坏/超限上传、TXT/PDF/DOCX解析、请求边界、缺模型、伪造引用、确认前不创建待办、重复及并发确认、字段编辑与持久化，以及模型 HTTP 契约/错误脱敏。

`python evaluate.py`：30/30 HTTP 成功，18/18 可回答题命中原文，12 道不可回答题中 7 道仍检索到相关片段。该现象保留为检索局限，不写作模型拒答通过。

`pip check`：依赖一致。测试框架对 httpx 发出上游弃用提示，但当前锁定版本执行通过；不代表未来版本兼容。

干净启动：实际执行 `start.ps1` 新建仓库内 `.venv`、安装锁定依赖并启动 127.0.0.1:8765 成功；使用独立连接核对原有示例资料与待办仍保存。

## 可访问性与未验证项

输入具备标签、按钮可键盘聚焦、可见焦点样式、状态区域使用 aria-live、弹窗使用原生 dialog，文本不依赖颜色单独表达含义。尚未完成屏幕阅读器人工测试、全量 WCAG 审计或跨浏览器覆盖，不以此声明全面无障碍合规。

2026-09-11 的验收未包含真实模型；2026-09-12 补验结果如下。独立人工语义评分、公开线上部署和多用户场景仍未验证。模型 HTTP 协议使用测试替身验证，与真实 Codex 调用严格分开。

## 结论

**本机原文检索、真实模型问答及人工确认待办流程可演示。** 2026-09-12 已发布到 GitHub，并按用户的招聘展示用途设为公开：`penpenguin1412/project-knowledge-assistant`；发布记录见 docs/PUBLISH.md。

## 2026-09-12 Codex 补验

- 执行 `start-codex.ps1`，官方 CLI 显示 ChatGPT 登录，原有 SQLite 资料和 3 条待办保留。
- 原有浏览器页面切换真实模型问答，提问“项目交付物有哪些？”，15 秒后显示 3 项成果及项目简报行 10 的逐字引用。加载期间禁用操作，完成后恢复；页面提示使用 Codex 额度。
- 真实会议提取返回 4 条待确认草稿，未知负责人和日期为空，待办仍为 3 条；未执行人工确认。
- `python tests.py`：21 项通过，包括 Codex 参数约束、JSON 解析、意外工具事件拒绝和忙碌状态；测试替身不消耗模型额度。
- 30 题真实流程：30/30 HTTP 成功，25 次 Codex 调用；18 题返回有引用的回答，12 题拒答，其中 5 题在检索阶段拦截。记录见 `reports/live-20260911T164510Z.json`，人工语义评价尚未进行。
